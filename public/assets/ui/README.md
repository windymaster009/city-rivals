Fantasy UI assets live here.

- `backgrounds/` for painted menu backdrops and map previews
- `buttons/` for exported button skins when replacing CSS buttons
- `frames/` for stone, parchment, and modal frame sprites
- `icons/` for inventory, settings, profile, and status icons
